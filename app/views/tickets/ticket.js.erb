// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.
//$("#all").html("<%= escape_javascript(render 'all') %>");
//$("#activities").html("<%= escape_javascript(render 'activities') %>");

//$('#product_details').html('<%= escape_javascript render ('index') %>');
//$('#provider_partial_event_list').html("<%=escape_javascript(render 'provider_event_list')%>");
