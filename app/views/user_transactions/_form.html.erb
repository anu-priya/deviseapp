<%= form_for(@user_transaction) do |f| %>
  <% if @user_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_transaction.errors.count, "error") %> prohibited this user_transaction from being saved:</h2>

      <ul>
      <% @user_transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_profile_id %><br />
    <%= f.number_field :customer_profile_id %>
  </div>
  <div class="field">
    <%= f.label :customer_payment_profile_id %><br />
    <%= f.number_field :customer_payment_profile_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
