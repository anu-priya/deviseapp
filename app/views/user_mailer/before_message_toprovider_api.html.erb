<table cellpadding="0" cellpadding="0">
  <tr>
    <td style="padding:12px;">
	 <table cellpadding="0" cellpadding="0">
	 	<tr>
			<td style="font-family:Arial, Helvetica, sans-serif; font-size:18px; color:#444444;" height="40"><b>Hi</b><% if !@activity.nil? && @activity.present? && !@activity.created_by.nil? && @activity.created_by.present? && @activity.created_by.downcase=='provider'%>
				<% if !@user.user_profile.nil? && @user.user_profile.present? && @user.user_profile!=''%>
					<% if !@user.user_profile.business_name.nil? && @user.user_profile.business_name.present? && @user.user_profile.business_name!=''%>
						<b><%=@user.user_profile.business_name %></b>,
					<%else%>
						<b><%=@user.user_name%></b>,
					<%end%>
				<%else%>
					<b><%=@user.user_name%></b>,
				<%end%>
			<%else%>
			<b><%=@user.user_name%></b>,
			<%end%></td>
		</tr>
	 	<tr>
			<td style="font-family:Arial, Helvetica, sans-serif; font-size:14px; color:#333333;" height="30"><%= @email.split("@")[0] if !@email.nil?%> has requested information about <%=@activity.activity_name unless @activity.nil? && @activity.activity_name.nil?%>. </td>
		</tr>
		<tr>
			<td style="font-family:Arial, Helvetica, sans-serif; font-size:14px; color:#333333;" height="45">"<%=@msg if !@msg.nil?%>"</td>
		</tr>
		<tr>
			<td style="font-family:Arial, Helvetica, sans-serif; font-size:14px; color:#333333;" height="30">You can reply to <%= @email.split("@")[0] if !@email.nil?%> at their email address <%= @email if !@email.nil?%></td>
		</tr>
		<tr>
			<td style="font-family:Arial, Helvetica, sans-serif; font-size:14px; color:#333333;" height="30">Kindly,</td>
		</tr>
		<tr>
			<td><span style="font-family:Arial, Helvetica, sans-serif; font-size:14px; color:#9c9b9b;">Your Famtivity Team</span></td>
		</tr>		
	</table>

   </td>
  </tr>
</table>